<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="BHFanShop.Views.Selections">
    <Grid>
        <Image Source="stadion.jpg" Aspect="AspectFill" />


        <VerticalStackLayout VerticalOptions="Center" Spacing="20" Padding="30">
            <Button Text="A REPREZENTACIJA" 
                HeightRequest="80"
                FontSize="20"
                BackgroundColor="Gold"
                TextColor="Black"
                Clicked="OnAClicked" />

            <Button Text="U21 REPREZENTACIJA" 
                HeightRequest="80"
                FontSize="20"
                BackgroundColor="#0046b8"
                TextColor="White"
                Clicked="OnU21Clicked" />
            <Button Text="U19 REPREZENTACIJA" 
                    HeightRequest="80"
                    FontSize="20"
                    BackgroundColor="#0046b8"
                    TextColor="White"
                    Clicked="OnU19Clicked" />   

            <Button Text="U17 REPREZENTACIJA" 
                    HeightRequest="80"
                    FontSize="20"
                    BackgroundColor="#0046b8"
                    TextColor="White"
                    Clicked="OnU17Clicked" />

            <Button Text="ŽENSKA SELEKCIJA" 
                HeightRequest="80"
                FontSize="20"
                BackgroundColor="#0046b8"
                TextColor="White"
                Clicked="OnWomanClicked" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>
